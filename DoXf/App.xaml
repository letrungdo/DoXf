<prism:PrismApplication
    x:Class="DoXf.App"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="clr-namespace:Prism.DryIoc;assembly=Prism.DryIoc.Forms"
    mc:Ignorable="d">
    <Application.Resources>
        <!--  Application resource dictionary  -->
        <ResourceDictionary>
            <!--<Style ApplyToDerivedTypes="true" TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{x:Static const:ColorConst.LightTextColor}" />
            </Style>
            <Style ApplyToDerivedTypes="true" TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{x:Static const:ColorConst.PrimaryColor}" />
                <Setter Property="BarTextColor" Value="{x:Static const:ColorConst.LightTextColor}" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{x:Static const:ColorConst.LightTextColor}" />
                <Setter Property="FontSize" Value="{x:Static utility:BaseSize.FontSize14}" />
            </Style>-->
        </ResourceDictionary>
    </Application.Resources>
</prism:PrismApplication>